# A lost Plugin faults and its unfinished attempts are interrupted

<!-- Generated by tests/e2e. Regenerate with ATLAS_E2E_UPDATE=1; review every change. -->

## 1. Submit work the fixture holds in progress

SDK `GET /dataset` with <operator key>

→ **200**
```json
{
  "id": "<id-1>",
  "writing_release": "0.1.0"
}
```

## 2. The Plugin process dies; Core records the loss

SDK `GET /plugins/fixture` with <operator key>

→ **200**
```json
{
  "availability": "faulted",
  "capabilities": [
    "fixture.work"
  ],
  "fault": "Core lost contact with the Plugin; unfinished Operation outcomes are unknown.",
  "id": "fixture",
  "release": "1.0.0"
}
```

## 3. The held attempt is interrupted, not completed

SDK `GET /plugins/fixture/operations/<id-2>` with <operator key>

→ **200**
```json
{
  "capability": "fixture.work",
  "created_at": "<time>",
  "dataset_id": "<id-1>",
  "error": "Core lost contact with the Plugin; unfinished Operation outcomes are unknown.",
  "id": "<id-2>",
  "input": {
    "hold": true,
    "label": "held"
  },
  "output": {
    "label": "held",
    "stage": "held"
  },
  "plugin_id": "fixture",
  "plugin_release": "1.0.0",
  "status": "interrupted",
  "submission_id": "<submission>"
}
```

## 4. New work is refused, and retrying the old submission returns the interrupted attempt

SDK `GET /dataset` with <operator key>

→ **200**
```json
{
  "id": "<id-1>",
  "writing_release": "0.1.0"
}
```

SDK `POST /plugins/fixture/operations` with <operator key>
```json
{
  "dataset_id": "<id-1>",
  "submission_id": "<new submission>",
  "capability": "fixture.work",
  "input": {
    "label": "new"
  }
}
```

→ **409**
```json
{
  "code": "plugin_unavailable",
  "message": "The Plugin is not admitting Operations."
}
```

SDK `POST /plugins/fixture/operations` with <operator key>
```json
{
  "dataset_id": "<id-1>",
  "submission_id": "<submission>",
  "capability": "fixture.work",
  "input": {
    "label": "held",
    "hold": true
  }
}
```

→ **202** · Location `/plugins/fixture/operations/<id-2>`
```json
{
  "capability": "fixture.work",
  "created_at": "<time>",
  "dataset_id": "<id-1>",
  "error": "Core lost contact with the Plugin; unfinished Operation outcomes are unknown.",
  "id": "<id-2>",
  "input": {
    "hold": true,
    "label": "held"
  },
  "output": {
    "label": "held",
    "stage": "held"
  },
  "plugin_id": "fixture",
  "plugin_release": "1.0.0",
  "status": "interrupted",
  "submission_id": "<submission>"
}
```
