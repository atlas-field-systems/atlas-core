# Concurrent identical enrollments create one Asset

<!-- Generated by tests/e2e. Regenerate with ATLAS_E2E_UPDATE=1; review every change. -->

## 1. Prepare an Asset identity

SDK `GET /dataset` with <enrollment key>

→ **200**
```json
{
  "id": "<id-1>",
  "writing_release": "0.1.0"
}
```

## 2. Send the same enrollment eight times at once

Observed response statuses, sorted:
```json
[
  200,
  200,
  200,
  200,
  200,
  200,
  200,
  201
]
```

Observed distinct response bodies:
```json
1
```
