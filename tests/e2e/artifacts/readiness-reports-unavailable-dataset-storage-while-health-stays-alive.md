# Readiness reports unavailable Dataset storage while health stays alive

<!-- Generated by tests/e2e. Regenerate with ATLAS_E2E_UPDATE=1; review every change. -->

## 1. Remove the Dataset table under the running Core

The operational database no longer has a dataset table.

## 2. Readiness reports SQLite unavailable

SDK `GET /readiness` with <operator key>

→ **503**
```json
{
  "dependencies": {
    "objects": "ready",
    "sqlite": "unavailable"
  },
  "status": "unavailable"
}
```

## 3. Health stays alive

SDK `GET /health` with <operator key>

→ **200**
```json
{
  "status": "alive"
}
```
